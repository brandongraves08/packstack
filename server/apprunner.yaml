version: 1.0
runtime: python3
build:
  commands:
    pre-build:
      - pip install -r requirements.txt
run:
  runtime-version: 3.8
  command: python app.py
  network:
    port: 8080
  env:
    - name: FLASK_ENV
      value: "production"
    - name: PORT
      value: "8080"
